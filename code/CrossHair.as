package code {		import flash.display.MovieClip;	import flash.events.*;			public class CrossHair extends MovieClip {				public var win:Boolean;				public function CrossHair() {			// constructor code			this.addEventListener(Event.ENTER_FRAME, frameLoop);			win = false;			//trace("Crosshair is here");//debug		}		//make the cursor follow the mouse around, vars				//private var speed:Number = 10;				private function frameLoop(e:Event):void		{			//make the cursor be at the mouse position			if(!win){				this.x = stage.mouseX;				this.y = stage.mouseY;			}else{				removeEventListener(Event.ENTER_FRAME,frameLoop);			}			/*//make the cursor follow the mouse around			///////////////this can be used for bulletime game//////////////			var yDistance:Number = stage.mouseY - y;			var xDistance:Number = stage.mouseX - x; 			if(Math.sqrt(yDistance*yDistance +  xDistance*xDistance) < speed){				x = stage.mouseX;				y = stage.mouseY;			}			else{				var radian:Number = Math.atan2(yDistance, xDistance);				x += Math.cos(radian) * speed;				y += Math.sin(radian) * speed;			}*/		}					}	}